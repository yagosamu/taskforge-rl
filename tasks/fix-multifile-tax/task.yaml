id: fix-multifile-tax
title: Fix the multi-file tax calculation
description: >
  Order totals include tax through the pricing module. The failing behavior is
  visible in orders, but the cause may be in another module.
workspace: workspace
tests:
  visible:
    - tests/test_basic.py
  hidden:
    - tests/test_grading.py
reward:
  type: test_pass_ratio
  partial_credit: false
budget:
  test_timeout_s: 10
limits:
  max_steps: 25
metadata:
  difficulty: hard
  tags:
    - multi-file
    - pricing
