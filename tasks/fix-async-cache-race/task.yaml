id: fix-async-cache-race
title: Fix the asyncio cache race
description: >
  The async cache should ensure concurrent requests for the same key share one
  in-flight computation instead of running the loader more than once.
workspace: workspace
tests:
  visible:
    - tests/test_basic.py
  hidden:
    - tests/test_grading.py
reward:
  type: test_pass_ratio
  partial_credit: false
budget:
  test_timeout_s: 10
limits:
  max_steps: 25
metadata:
  difficulty: hard
  tags:
    - asyncio
    - cache
    - race-condition
