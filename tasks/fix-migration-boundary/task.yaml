id: fix-migration-boundary
title: Fix migration boundary rows
description: >
  The migration should archive rows at or before the cutoff date, not just rows
  before it.
workspace: workspace
tests:
  visible:
    - tests/test_basic.py
  hidden:
    - tests/test_grading.py
reward:
  type: test_pass_ratio
  partial_credit: false
budget:
  test_timeout_s: 10
limits:
  max_steps: 25
metadata:
  difficulty: hard
  tags:
    - data-migration
    - boundary
