id: extract-discount-helper
title: Extract discount logic without breaking invoices
description: >
  The invoice module needs a reusable discount helper. Implement it while
  preserving the existing invoice total behavior.
workspace: workspace
tests:
  visible:
    - tests/test_basic.py
  hidden:
    - tests/test_grading.py
reward:
  type: test_pass_ratio
  partial_credit: false
budget:
  test_timeout_s: 10
limits:
  max_steps: 25
metadata:
  difficulty: medium
  tags:
    - refactor
    - helper-extraction
