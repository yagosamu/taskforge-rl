id: optimize-common-ids
title: Optimize common id lookup
description: >
  common_ids returns ids present in both lists while preserving the order of the
  first list. The current implementation is too slow for large inputs.
workspace: workspace
tests:
  visible:
    - tests/test_basic.py
  hidden:
    - tests/test_grading.py
reward:
  type: test_pass_ratio
  partial_credit: false
budget:
  test_timeout_s: 10
limits:
  max_steps: 25
metadata:
  difficulty: hard
  tags:
    - performance
    - runtime-bounded
