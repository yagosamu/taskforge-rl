id: stateful-second-call
title: Make batch collection independent across calls
description: >
  collect_batches(items, size) must return a list of lists, each inner list
  containing up to size items from the input order. The function must be
  stateless: calling it a second time must not include batches or items from any
  previous invocation.
workspace: workspace
solution: solution
tests:
  visible:
    - tests/test_basic.py
  hidden:
    - tests/test_grading.py
reward:
  type: test_pass_ratio
limits:
  max_steps: 25
metadata:
  difficulty: hard
  tier: 2
  tags:
    - stateful
    - second-call
    - data-structure
